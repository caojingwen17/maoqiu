.ua {
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  background: var(--fill);
  flex: none;
}
.ua-round { border-radius: 50%; }
.ua-sq { border-radius: 36rpx; }
.ua-img { width: 100%; height: 100%; }
.ua-sketch { width: 62%; height: 62%; opacity: 0.7; }