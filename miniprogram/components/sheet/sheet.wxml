<block>
  <view class="sheet-mask {{show ? 'show' : ''}}" bindtap="onMask"></view>
  <view class="sheet {{show ? 'show' : ''}}">
    <view class="grip" catchtap="noop"></view>
    <slot></slot>
  </view>
</block>