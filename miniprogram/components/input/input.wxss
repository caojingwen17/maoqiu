.input-row .k {
  width: 167rpx;
}
.ival {
  flex: 1;
  min-width: 0;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 9rpx;
  text-align: right;
}
.ival input {
  flex: 1;
  min-width: 0;
  text-align: right;
  font-size: 31rpx;
  color: var(--t2);
  height: 1.4em;
  line-height: 1.4em;
}
.ival input.b {
  color: var(--text);
  font-weight: 500;
}
.ival .unit {
  font-size: 31rpx;
  color: var(--t3);
  flex: none;
}
.ival input.ph {
  color: var(--t3);
}