# 毛球档案袋 · 产品架构图与数据流图

| 项目 | 内容 |
|---|---|
| 对应文档 | PRD v1.1 / 设计语言 v2.2 / 页面审查与优化报告 v1 |
| 文档版本 | v1.0 |
| 文档日期 | 2026-08-14 |
| 用途 | 开发前的全局视图：模块怎么分层、数据怎么流动、联动在哪发生 |

> 本文所有图为 Mermaid，可直接在支持 Mermaid 的 Markdown 阅读器（VS Code 插件 / Typora / GitHub）中渲染。

---

## 1. 产品架构图（功能分层视图）

```mermaid
graph TD
  subgraph L1["① 展示层 · 4 个 Tab + 页面栈"]
    TAB1["Tab 宠物 · 首页卡片墙<br/>待办聚合 / 统计速览 / 快捷记录"]
    TAB2["Tab 提醒 · 提醒中心<br/>进行中 / 已完成 / 新建护理提醒"]
    TAB3["Tab 统计 · 数据图表<br/>体重 / 花销 / 打卡热力"]
    TAB4["Tab 我的<br/>家庭 / 囤货 / 记账 / 归档 / 工具箱"]
    P1["宠物详情页<br/>时间线 · 健康 · 花销 · 相册"]
    P2["档案编辑页 / 新增宠物"]
    P3["18 种记录表单<br/>共用骨架 + 类型专属字段"]
    P4["记录详情页"]
    P5["记账页 / 体重详情页 / 相册页"]
    P6["囤货页 + 入库表单"]
    P7["家庭成员页 / 邀请落地页"]
    P8["归档宠物"]
    P9["工具箱<br/>年龄换算 · 喂食量 · 安全食物 · 附近医院"]
    TAB1 --> P1 --> P2 & P4 & P5
    TAB1 & P1 --> P3
    TAB2 --> P3
    TAB4 --> P6 & P7 & P8 & P9
  end

  subgraph L2["② 业务逻辑层 · 云函数（唯一数据出入口）"]
    F1["pet.* 档案 CRUD<br/>级联删除 / 归档"]
    F2["record.* 记录 CRUD<br/>乐观锁校验"]
    F3["reminder.* 提醒 CRUD<br/>完成 / 延后 / 去重推进"]
    F4["inventory.* 入库 / 扣减 / 临期扫描"]
    F5["family.* 邀请 / 加入 / 退出 / 解散"]
    F7["stats.* 聚合统计"]
    F8["home.aggregate 首页聚合<br/>（1 次调用拿全首屏）"]
    F9["定时任务 cron<br/>当前关闭，微信提醒后续评估"]
  end

  subgraph L3["③ 数据层 · 云开发"]
    DB1[("pets 宠物")]
    DB2[("records 记录<br/>18 类型统一集合")]
    DB3[("reminders 提醒")]
    DB4[("inventories 囤货")]
    DB5[("families 家庭空间")]
    DB6[("settings 用户设置")]
    ST[("云存储<br/>头像 / 记录照片")]
  end

  subgraph L4["④ 外部能力"]
    WX2["微信分享卡片<br/>家庭邀请"]
    WX3["腾讯地图<br/>附近医院检索"]
  end

  L1 -->|"所有读写走云函数<br/>客户端零直连数据库"| L2
  L2 --> L3
  F5 --> WX2
  P9 --> WX3
```

**分层要点：**

- 展示层任何页面**不直连数据库**，这是家庭共享权限模型（members 校验）的前提，也是 4.7 的硬约束。
- `home.aggregate` 一个云函数返回首屏全部数据（宠物卡 + 待办 + 统计速览 + 成员），保证冷启动 ≤2s。
- 工具箱是纯前端模块（年龄换算 / 喂食量 / 安全食物为本地计算 + 离线 JSON），只有附近医院调外部接口，**不依赖登录态，首次进入即可用**——它是新用户体验产品的零成本入口。

---

## 2. 核心数据流图

### 2.1 记一笔 → 全链路联动（产品最核心的一条流）

```mermaid
flowchart TD
  U["用户：快捷记录九宫格<br/>（首页 FAB / 卡片 + / 待办完成入口）"] --> F["记录表单<br/>选宠物 → 填字段 → 保存"]
  F --> CF["云函数 record.create<br/>① members 权限校验<br/>② 写入 records"]
  CF --> R[("records")]

  CF --> Q1{类型联动判断}
  Q1 -->|"vaccine / deworm"| A1["创建或更新周期提醒<br/>同宠同类只留 1 条 active（去重）"]
  Q1 -->|"medication"| A2["生成起止期内每日服药待办<br/>每日 N 次，打卡写 checkins"]
  Q1 -->|"feed"| A3["匹配 category=food 库存<br/>remainAmount -= 克数"]
  Q1 -->|"deworm / medication"| A4["匹配 linkType 库存<br/>remainAmount -= 1 件"]
  Q1 -->|"expense"| A5["计入当月花销聚合<br/>超预算时首页速览变橙"]
  Q1 -->|"weight"| A6["更新宠物卡最新体重<br/>偏离品种区间 ±20% 提示"]

  A1 & A2 --> RM[("reminders")]
  A3 & A4 --> CHK{余量检查}
  CHK -->|"≤ 阈值 或 撑不过 7 天"| A7["创建补货提醒（触发一次）"]
  A7 --> RM
  CHK -->|"expireDate 临期"| A8["小程序内生成临期待办"]

  R --> V["回流展示：时间线插入新行<br/>宠物卡数据更新 · 统计图更新"]
  RM --> V2["回流展示：首页待办区 · 提醒 Tab"]
```

### 2.2 提醒展示流（仅小程序内）

```mermaid
flowchart LR
  OPEN["用户打开小程序"] --> Q["读取 status=active<br/>且到期的提醒"]
  Q --> APP["首页待办 + 红点 + 提醒中心"]
  APP --> ADV{周期提醒?}
  ADV -->|是| NEXT["remindAt += 周期"]
  ADV -->|否| DONE["status = done"]
```

### 2.3 家庭共享流

```mermaid
flowchart TD
  O["创建者：成员页点邀请"] --> SH["wx.shareAppMessage<br/>小程序卡片携带 familyId"]
  SH --> J["被邀请人：打开落地页<br/>显示空间名 / 宠物封面 / 成员头像"]
  J --> C{"云函数 family.join 校验"}
  C -->|"成员 < 5 且未在他空间"| OK["写入 members<br/>即可见全部历史数据"]
  C -->|"已在其他空间"| E1["提示先退出 + 直达退出入口"]
  C -->|"满员"| E2["提示最多 5 人"]
  OK --> W["协作写入：record.create 带 createdBy/createdByName 快照<br/>update 前校验 updateAt 乐观锁"]
  W --> PUSH["家庭成员打开小程序后<br/>看到同一份待办与记录"]
```

### 2.4 数据归属关系（一图看懂权限）

```mermaid
erDiagram
  families ||--o{ pets : "familyId"
  families ||--o{ records : "familyId"
  families ||--o{ reminders : "familyId"
  families ||--o{ inventories : "familyId"
  pets ||--o{ records : "petId"
  pets ||--o{ reminders : "petId（全局提醒可空）"
  pets ||--o{ inventories : "petId（通用物品可空）"
  records |o--o| reminders : "sourceRecordId（哪条记录生成的提醒）"
  inventories |o--o{ records : "linkType 联动扣减"
```

> 权限规则：云函数内校验「请求人 openid ∈ families.members」；`_openid` 仅用于审计和「我创建的」筛选，不再是隔离边界（v1.1 已修正 PRD 4.3/4.4 与 4.6 的矛盾）。

---

## 3. 云函数清单（开发接口边界）

| 云函数 | 职责 | 关键规则 |
|---|---|---|
| `home.aggregate` | 首屏聚合 | 1 次调用返回宠物卡 + 待办 + 统计速览 + 成员头像 |
| `pet.create / update / remove / archive` | 档案 | remove 级联删 records/reminders/inventories + 云存储图片；archive 同步停用其全部 active 提醒 |
| `record.create / update / remove` | 记录 | 写入后执行 §2.1 联动；update 乐观锁；remove 时若存在 sourceRecordId 派生提醒则提示「关联提醒将一并处理」 |
| `reminder.create / update / complete / postpone / disable` | 提醒 | complete 周期推进；同宠同 category 去重 |
| `inventory.inbound / consume / update` | 囤货 | consume 后做阈值与临期检查 |
| `family.invite / join / leave / removeMember / dissolve` | 家庭 | join 校验名额与唯一空间；dissolve = 创建者退出 |
| `stats.summary` | 统计聚合 | 按月/半年/年聚合，图表数据 |
| `cron.hourly` | 预留定时任务 | 当前关闭；微信提醒接入时再评估 |

---

*文档结束 · 产品架构图与数据流图 v1.0*
